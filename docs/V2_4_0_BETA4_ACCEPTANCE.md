# Engineering 2.4.0-beta.4 acceptance

Release candidate for script-call dependency analysis.
Base: `76b09d48900e05b910f6d23b815ce07b3d956312` (beta.3).
No gate is asserted passed by this document. Preserve all earlier acceptance
receipts and their qualifications; do not repeat completed beta campaigns.

## Source and disposable gates

Require clean final-head Full/Evidence, exact-head CI and a separately tasked
independent implementation/security review. All three authoritative versions must
resolve to 2.4.0-beta.4; staging must be consumed; active documents must resolve
exactly. Josh's later Ready action controls protected merge/publication. Deployment
and installed read-only validation require their separate authorization.

Prove exact renamed-script mapping, explicit targets, automation → script → entity
and three-edge chains, distinct paths and source filtering. Prove no inherited
output effects for state-only script observers, no prose false positives,
parse-only templates, bounds/cycles, unreadable neighbors, unknown/dynamic calls,
redaction, cache/fingerprint/cursor retirement and unchanged helper/impact bindings.
A zero result with incomplete script discovery must not claim absence.

Run the existing exact disposable Core 2026.9.3 / ha-mcp 8.5.0 lane with the new
script scenario. Verify actual renamed service registration and two-/three-edge
paths through the admitted reader, feature-boundary read capture, fault isolation
and exact disposable cleanup. Do not execute the scripts. Prepared test source or
offline synthetic providers do not establish executed integration acceptance.

Expected inventory remains 79 = 54 static + 25 delegated; Core authority remains
19 compatible profiles using the existing reviewed signed authority. The tool
input schema is unchanged; description and additive diagnostic output change.
No registry, workflow, dependency, deployment permission or storage-format change.

## Later installed gates

After separate deployment/read authorization, bind the running image's
architecture/configuration digest chain to the verified publication receipt.
Verify exact source/version/build, REST/WebSocket Core agreement, exact ha-mcp
admission, signed authority, storage/audit health and settled execution. Matching
labels alone are insufficient image evidence. Stop dependent checks on drift.

Preserve native/delegated useful-read and provider/completeness/fallback evidence.
Retain a fresh public initialize → initialized → all tools/list pages session with
zero tools/call and separate contemporaneous runtime brackets. Verify all 79 tools
and the changed dependency description. Cached connector inventory is not raw
capture; no new connector is required solely by unchanged tool inputs. Confirm
the existing approval panel renders without creating anything.

Select one existing, already identified automation/script/entity chain. Establish
its canonical storage keys, current entity IDs and exact configuration paths via
bounded supported reads; do not create a production fixture. Query its target with
include_indirect=true, max_depth=3, bounded pagination and refresh_index=false.
Verify useful caller findings, per-page path evidence, actual provider attribution
and truthful partial coverage. A source_types=[automation] query must retain
script intermediates. Repeat once to verify a cache hit and stable findings when
state has not changed. Missing suitable fixture evidence remains an explicit gap.
No forced refresh loop, script execution, device cycle or dashboard write.

Finish with no-probe health and reconcile executions/tasks, approvals, locks,
holds, leases, commits, recovery and fallback with pre-state. Historical counters
are not new failures. Do not restart beta.3 dashboard write/restoration acceptance;
its remaining live decisions remain separately bounded and authorized.

## Recovery

A reviewed source revert restores prior diagnostic behavior. No persistent format
changes are introduced. Before any deployment, retain an actual compatible image
and configuration recovery path; do not restore stale execution records merely
to satisfy acceptance. Report unavailable receipts, incomplete coverage and
verification failures explicitly; source tests do not prove installed behavior.
