# Engineering 2.3.0-rc.1 acceptance

This candidate stabilizes the existing 2.3 functionality and exercises protected
automatic publication. It adds no operation family, tool, provider, dependency or storage
format. Publication, installation and household actions retain distinct authority.

## Source and release boundary

Readiness baseline is main `2dbb6e8d2948f9cd5e3fe876c0f84311ae5c169f`.
Published beta.4 is `c07bc54ae0cf9d52a3bd205f2a24de704f7b8296`.
Their Engineering runtime, controlled inputs and deployment configuration are
identical. The intervening changes are reviewed Core .3 signed registry data and
delivery workflow/validation changes. Resolve actual release base, candidate and
merged identities when they exist; none is inferred from these prior revisions.

Require canonical beta.4 -> rc.1 sequencing. Author the temporary declaration,
require exact staged acceptance and release-notes resolution, inspect promotion
preview, then materialize all three version authorities in the same PR and
consume the declaration. Require exact active resolution and clean-head Evidence
against the complete PR base. Declare every changed protected surface explicitly.

Runtime delta from the readiness baseline is limited to SERVER_VERSION. Preserve
78 tools (53 static and 25 delegated reads), schemas/annotations, provider routes,
Core profile fingerprints, signed journals, approval meaning, dispatch/recovery,
persistence readers, stable-v1 1.1.2, controlled inputs, technical Beta installation
identity, image repository, ports, ingress, options and both supported platforms.

Run the complete existing source suite and exact-head CI, including real Core
.3/ha-mcp 8.5.0 and historical supported pairings on their declared native
architectures. Preserve useful success, exact readback, duplicate suppression,
stale/invalid-authority refusal, injected-loss recovery and separate restoration.
Bind actual test checkout or synthetic merge parents/tree to the reviewed head.
Report every skip and limitation. Obtain one independent release-delta review
before Josh's Ready decision. Do not relabel old evidence as a new candidate pass.

## First automatic publication

Josh's Ready decision on the reviewed materialized release PR authorizes the
existing protected exact-head merge/publication flow. Verify that the handoff
binds that decision, PR/head, protected base, merged source and exact version.
Observe the actual `workflow_run` path through complete validation to publication.
The successful non-release workflow 35638306215 is supporting evidence only.

Require one owned publication attempt and independent final readback:

- `v2.3.0-rc.1` annotated tag targets the exact merged release revision.
- GitHub Release is a prerelease with canonical rc.1 notes.
- Version and commit image tags resolve to the same immutable index.
- Both linux/amd64 and linux/arm64 manifests/configurations, build/source/clean
  identity, controlled dependency inventory, SBOM and provenance pass the existing
  exact-source verifier, including the original automatic handoff binding.
- Retain run/attempt, source and digest chain so installed acceptance can bind it.

A successful source validation or image push alone is not publication success.
On partial publication preserve the marker, original source binding and any
uploaded digest; stop for bounded reconciliation. Do not blindly rerun, rebuild,
claim again, overwrite tags or silently take the manual route. Digest recovery,
if justified, needs the reviewed original evidence and an exact owner decision.
Do not inject publication failures into production to manufacture acceptance.

## Retained functional evidence and qualifications

Carry beta.4 disposable integration and persisted compatibility evidence with
their exact source/pairing/time. Carry beta.3 lamp/switch and earlier fan/dashboard
canaries with their original evidence attribution. Carry beta.4 installed-image
binding and September 20 Core .3 read-only continuity as historical evidence.
These do not establish the RC installation.

Preserve the beta.3 raw-catalog runtime-bracket qualification. Supplied results
remain supplied where raw envelopes were not independently available. Metadata
inventory is not raw enumeration. HA state is not physical feedback; dashboard
saves remain non-atomic against external editors. Script/scene/group/template/
dashboard dependency configuration coverage remains unavailable and dynamic
references opaque. No-result analysis cannot establish absence of dependencies.

The single upgrade-associated authority failure recovered; the precise exception
remains unknown. The two retained-effect records counted as historically
compatible are distinct from the three unreadable plans reproduced during rc.1
acceptance. The latter comprise one expired helper snapshot and two rejected
automation-planning records; they are historical read-projection findings, not
newly failed executions. See [the bounded correction](TERMINAL_HISTORY_PROJECTION_CORRECTION.md)
for their diagnosis and separate source-validation scope; this does not relabel
the installed rc.1 runtime as fixed. Complete Android navigation,
cache-only outage startup and independent backup-content/restore verification
remain unestablished. F3 observation counters do not establish diagnostic-observer
dormancy. Do not expand or silently clear these qualifications.

## Separately authorized installation and bounded read-only acceptance

Keep the existing single Engineering connector and add-on. Core remains the
accepted 2026.9.3 with internal ha-mcp 8.5.0; no Core/OS/proxy/provider update or
trust change accompanies this candidate. Before deployment, preserve current
configuration and execution state and identify the compatible recovery artifact.

After Josh authorizes deployment and confirms installation:

1. Bind running container/image configuration through the platform manifest to
   the independently verified rc.1 publication index. Require exact release
   source/version/build and dirty=false. Runtime labels alone do not close this.
2. Read server_info and health without initiating unnecessary probes; one bounded
   connectivity check if needed. Require REST/WebSocket Core identity agreement,
   exact ha-mcp admission, accepted/current signed authority, 19 admitted profiles
   on Core .3, healthy audit/storage and F3. Record actual sequence/expiry and
   compare with the reviewed journal; don't require a remembered sequence blindly.
3. Capture one fresh public initialize/initialized and complete paginated
   tools/list session, zero tools/call, through the existing authorized connector.
   Bracket contemporaneously with identity/authority/health. Compare all 78 unique
   descriptors to the exact release/provider reference. Record actual protocol
   and client-cache limitations. Missing raw evidence leaves this gate qualified;
   a supported inventory count cannot substitute.
4. Perform one useful native read and one admitted delegated read on the same
   established read-only fixture, preserving exact provider, completeness,
   truncation, fallback and result facts. Do not operate a device.
5. Let ordinary dependency prewarm settle. If needed, issue at most one bounded
   ordinary dependency request with refresh_index=false; no forced builds or
   repeated retries. Preserve truthful partial coverage.
6. Check existing historical receipt continuity and normal approval-panel access
   without creating plans/challenges/notifications. Record inaccessible evidence.
7. Finish with no-probe health. Reconcile active executions/tasks, locks, holds,
   leases, commits, pending approval/rollback work, recovery/audit/storage errors
   and fallback against pre-state. Distinguish retained counters from new failures.

Report each gate separately, including partial, unavailable and verification
failure outcomes. An identity/authority mismatch stops dependent acceptance until
reconciled. Unknown tools or a stale connector label do not prove server downtime.
Never redispatch or substitute a provider to recover missing evidence. No new
household canary is required without a specific compatibility question and exact
owner-approved fixture/action/restoration plan.

## Recovery and stable promotion

Beta.4 is the proposed compatible binary recovery reference only while final
source/format equivalence remains proven and exact artifact availability verified.
Reconcile active/uncertain work and preserve the current execution store, IDs,
receipts and holds. Downgrading to beta.3 cannot read new Core-bound records.
Restoring older execution state can lose dispatch knowledge and must not be used
as routine rollback. A binary change, state restoration, Core rollback and
physical restoration are distinct actions with separate owner decisions.

After actual automatic publication and bounded installed RC acceptance pass with
all qualifications explicitly retained, prepare the separate version-only
rc.1 -> 2.3.0 stable transition with exact stable documents, candidate validation,
review and Josh's Ready decision. Rebind the stable publication and installed
identity; reuse applicable functional evidence without inventing a new campaign.
Any functional change or incompatible recovery assumption reopens its affected
validation and release-scope decision.
