# Engineering 2.3.0-rc.2 acceptance

RC2 carries the bounded historical projection correction into the feature-frozen
2.3 release. It adds no operation family, provider, tool, dispatch route or storage
format. Source preparation, Ready/merge/publication, deployment and live checks
remain distinct authority boundaries.

## Source, scope and exact candidate

The published rc.1 baseline is `df860eeffdc9031bd0596a9a9892b9e1c9c08f1e`.
The independently reviewed correction is
`92104ece78e71fcb5e119f085a92e2976bd75d16`. Resolve and record the actual fetched
base, final candidate and eventual merged release revision; these earlier SHAs
do not substitute for the final release identity.

Require canonical rc.1 -> rc.2 sequencing. Author the single-use declaration,
prepare these exact release documents, require exact staged context resolution,
inspect the promotion preview and materialize all three version authorities in
the same PR. Consume the declaration, require exact active document resolution
and run clean-head Full/Evidence against the complete PR base with each changed
protected file declared. No unconsumed declaration or post-merge version commit.

Beyond the version, runtime changes are confined to historical read projection
and compatibility accounting in governance/service.py and historical_policy.py.
Preserve current policy, approval/apply/rollback/external decision/recovery
authority, public tool schemas and registration, provider routes, signed journals,
Core profile fingerprints, persistence writers, dependencies, workflow permissions,
stable-v1 1.1.2 and installation configuration. The existing compatibility count
map gains only the three documented profile keys; its authorization effect remains
`none_projection_only`.

The accompanying [guidance reconciliation](INSTRUCTION_GUIDANCE_RECONCILIATION.md)
requires a bounded review of the instruction/documentation delta and its affected
checks. Keep the original runtime correction and fixture identities separately
verified. The final candidate needs fresh clean-head Evidence and exact-head CI;
prior-head passes remain historical evidence. The guidance delta requires no
additional live or household canary. Do not claim the desktop entrypoint or
unavailable private audit artifacts were independently verified.

The expected reviewed catalog is 78 tools (53 static, 25 delegated reads).
Core 2026.9.3 / ha-mcp 8.5.0 remains the installed acceptance pairing with all
19 Core profiles admitted under valid authority. Preserve Core .2's original
17-reference route and all other compiled supported pairings. No Core, OS,
proxy, provider or signed-registry change accompanies RC2.

## Useful success, refusal and independent review

Follow [the correction contract](TERMINAL_HISTORY_PROJECTION_CORRECTION.md).
Retain independent byte-for-byte fixture/provenance regeneration from the exact
shipped beta.38 and beta.4 writers, sanitized before hashing. Do not reconstruct
historical records or edit hash-bearing output by hand. Preserve the unmodified
rc.1 negative control and the correction's existing independent review evidence.

Require useful get/list reads of the three fixture shapes with original terminal
status, policy and hashes; truthful mixed compatibility/failure accounting;
restart/deep-audit continuity; and unchanged persisted bytes with zero task or
provider dispatch. Tampered hashes, unsupported or extended lifecycles, approval,
task, dispatch or recovery evidence, secret-bearing or impossible writer proposals,
and malformed values must retain bounded refusals. Task-storage failure stays
fatal. Existing legitimate approved execution, readback and recovery regressions
must also pass. Never exercise apply against live historical records to prove
this read-only correction; rejected apply can append its original refusal audit.

Run complete candidate Evidence and exact-head CI: source, metadata, packaging
on both supported architectures, disposable Core contracts, exact-image gateway
and exact add-on runtime lanes. Report pass/skip/unavailable separately and bind
the actual checkout or verified merge parents/tree to the reviewed candidate.
After Full/Evidence, obtain a separate independent AI review of the release delta;
identify reviewer scope, exact commit and evidence. Carry the prior correction
review only while its code/fixtures remain unchanged. Keep the PR draft for Josh.

## Publication and retained evidence

Josh's Ready decision on the reviewed materialized PR authorizes the existing
protected exact-head merge/publication flow, not deployment. Verify its original
handoff and one owned publication attempt. Require independent final readback:

- Annotated `v2.3.0-rc.2` tag targets the exact merged revision.
- GitHub prerelease contains the canonical RC2 notes.
- Version and commit image tags resolve to the same immutable index.
- Both linux/amd64 and linux/arm64 manifests/configurations, clean source/build
  identity, dependency inventory, SBOM and provenance pass the exact-source
  verifier, including the original automatic handoff binding.
- Retain source, run/attempt and index/manifest/configuration digests for installed
  binding; source labels and a successful image push alone are insufficient.

Partial publication requires bounded reconciliation of the original claim/source
and any uploaded digest. Preserve failed evidence; do not blindly retry, rebuild,
claim again, overwrite tags or substitute manual publication. Recovery requires
its separately reviewed original binding and exact owner decision.

Carry applicable rc.1/beta installed-image, raw-catalog, useful-read, canary and
receipt evidence with their original revision, pairing, time and attribution.
Revalidate retained manifests when available; mark inaccessible artifacts as
unavailable. They do not prove an RC2 installation. Preserve beta.3's raw-catalog
runtime-bracket qualification; later observations cannot repair it. Counts or
cached schemas are not fresh protocol enumeration.

The two retained-effect compatibility records are distinct from the three rc.1
unreadable records. Those three were an expired helper and two failed planning
lifecycles, with no associated execution tasks. Their source correction does not
prove production readback. Preserve the upgrade-associated authority exception's
unknown root cause, incomplete dependency coverage/dynamic opacity, non-atomic
dashboard saves, HA-state versus physical-feedback distinction, and unestablished
complete Android navigation, cache-only outage startup and independent backup
content/restore verification. F3 counters alone do not prove observer dormancy.

## Separately authorized installed read-only acceptance

Before deployment, identify the exact approved artifact, preserve configuration
and current execution state, and verify the compatible recovery artifact. After
Josh authorizes deployment and read-only acceptance and confirms installation:

1. Bind the running image configuration and platform manifest to the independently
   verified RC2 publication index. Require exact version/source/build and
   dirty=false; a matching version/SHA label alone does not close this gate.
2. Read server_info and health without unnecessary probes; use one bounded
   connectivity check if needed. Confirm REST/WebSocket Core .3 agreement,
   exact ha-mcp 8.5.0 admission, accepted/current/valid signed authority and all
   19 profiles, healthy storage/audit and F3 readiness. Compare observed registry
   sequence/expiry with the reviewed unchanged journal, not a remembered number.
3. Capture one fresh public initialize/initialized and complete paginated
   tools/list session, zero tools/call, through the existing authorized Nabu Casa
   path. Bracket with contemporaneous identity/authority/health and compare all
   78 unique descriptors to exact release/provider references. Keep endpoint and
   authentication material private. Missing raw evidence qualifies this gate.
4. Perform one native and one admitted delegated useful read on the established
   same fixture. Preserve provider attribution, completeness, truncation and
   fallback facts. Allow dependency prewarm to settle; if necessary use at most
   one ordinary bounded request with refresh_index=false, never forced rebuilds.
5. Use the original private evidence to identify the three retained plans. Read
   each once through supported tools. Require the original expired or
   validation_failed status, original policy and hash bindings where exposed,
   no tasks or revived approval/dispatch authority, and unchanged raw-record
   integrity where the existing authorized operator evidence permits comparison.
   Report unavailable fields explicitly; do not invent readback coverage or
   rerun raw collection without its applicable authorization.
6. Reconcile compatibility profile counts separately from genuine projection
   failures. The expected three corrections do not justify clearing unrelated
   failures or resetting counters. Confirm prior successful receipts remain
   readable and normal approval-panel access without creating or approving work.
7. Finish with no-probe health: reconcile active tasks/executions, locks, holds,
   leases/commits, pending approval/rollback work, storage/audit/recovery errors
   and fallback against pre-state. Distinguish historical counts and diagnostic
   read failures from new operational failures.

Report identity/image, catalog, connectivity/authority, original-record readback,
storage/history, dependency and final settlement as separate outcomes. Stop
dependent checks on an identity/authority mismatch. No device cycle, plan creation,
approval, apply, restart, configuration write or alternate production route belongs
to this acceptance. Unknown tool responses or stale connector labels alone do not
establish downtime. An unavailable mandatory gate leaves acceptance qualified.

## Recovery and stable promotion

There is no migration or read-time record rewrite. Removing this fix restores
the former historical read limitation; it is not a record restoration. A binary
recovery needs independently verified compatible code, formats and artifact
availability, preserving current receipts, IDs, holds and any uncertain dispatch.
Beta.3 remains incompatible with newer Core-bound fan/power records. Restoring
older execution state may erase dispatch knowledge and is not routine rollback.
Binary changes, storage restoration, Core rollback and household restoration are
separate owner decisions.

Only after publication and bounded RC2 installed acceptance close, with previous
qualifications retained, prepare the separate version-only rc.2 -> 2.3.0 stable
transition. Require exact stable documents, candidate validation and independent
review before Josh's Ready decision. Rebind stable publication/installed identity
and reuse applicable functional evidence without restarting the beta campaign.
No new feature work precedes that stable objective.
