awesomeversion==25.8.0
# Preserve this test pin within MCP's range. Both complete hash locks now pin
# the same runtime httpx version; review both locks when that range changes.
httpx==0.28.1
pip-audit==2.10.1
